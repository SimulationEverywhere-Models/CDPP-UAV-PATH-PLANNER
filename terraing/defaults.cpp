/**
 * @file defaults.cpp
 * @author Keith Holman
 * @date Created: Wednesday, November 12, 2008
 * @brief Defines default values
 */

#include "defaults.h"

const unsigned int DEFAULT_COUNT = 15u;
const Value DEFAULT_MIN = static_cast< Value >( 1 );
const Value DEFAULT_MAX = static_cast< Value >( 7 );
const unsigned int DEFAULT_RANGE = 20u;
const Value DEFAULT_VALUE = static_cast< Value >( 0 );
